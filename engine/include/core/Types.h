#pragma once

// Tipos de vetores para coordenadas 2D
struct Vector2f {
    float x, y;
};

struct Vector2i {
    int x, y;
};

// Tipo de vetor para cores RGBA (0-255)
struct Vector4 {
    unsigned char r, g, b, a;
};
